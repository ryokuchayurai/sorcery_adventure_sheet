name: sorcery_adventure_sheet
description: Steve Jackson's Sorcery Adventure Sheet Apps.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^1.0.0
  shared_preferences: ^2.0.9
  routemaster: ^0.9.5
  numberpicker: ^2.1.1
  json_annotation: ^4.3.0
  freezed_annotation: ^1.0.0
  flutter_svg: ^0.23.0+1
  audioplayers: ^0.20.1
  google_fonts: ^2.1.0
  universal_platform: ^1.0.0+1
  url_launcher: ^6.0.17
  charts_flutter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.1.5
  json_serializable: ^6.0.1
  freezed: ^1.0.0
  flutter_gen_runner: ^4.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/
